"use strict"

const
  Dbus= require( "dbus-native"),
  xos= require( "xml-object-stream-sax")()

const function Introspection(introspection){
	return this.done= xos(xml, "//node").then(nodes => {
		for(var member of nodes.children){
			switch(member.tagName){
				case "method":
					this.
					break
				case "signal:
					break
				default:
					break
			}
			if(member.tagName === "method"){
			}else if(member.tagName == 
		}
	}).then(() => return this)
}
